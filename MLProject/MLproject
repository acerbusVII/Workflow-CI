name: diabetes-mlflow

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset:
        type: string
        default: "diabetes_preprocessing/train_preprocessing.csv"

    command: "python modelling.py {dataset}"
